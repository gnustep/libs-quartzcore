# A renderer here is built from an NSOpenGLContext where Apple builds one
# from a CGL context, so this cannot be run against Apple.
APPLE_SKIP_TESTS="renderer.m render.m rasterize.m shadow.m cache.m tint.m"
